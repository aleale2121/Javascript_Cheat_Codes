'use strict'; 
 var max = 100;
console.log(max);
 var max = 200;
 console.log(max);

// It’s poor programming practice to redefine a variable in the same scope as that
// often leads to errors in code.